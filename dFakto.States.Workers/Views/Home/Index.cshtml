@model IndexModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <ul>
        
    @foreach (var p in Model.Plugins)
    {
        <li>@p.ToString()</li>
    }
    
    </ul>
</div>